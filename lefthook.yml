# Reference:
# https://github.com/evilmartians/lefthook/blob/master/docs/full_guide.md

pre-push:
  commands:
    stylelint:
      run: yarn stylelint
    eslint:
      run: yarn lint

commit-msg:
  commands:
    commitlint:
      run: yarn commitlint --edit "$1"
