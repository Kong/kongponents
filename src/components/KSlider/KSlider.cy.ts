import { mount } from 'cypress/vue'
import KSlider from '@/components/KSlider/KSlider.vue'

describe.skip('KSlider', () => {
  it('TODO: This is just an example test', () => {
    mount(KSlider)
  })
})
